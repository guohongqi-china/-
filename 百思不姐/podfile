platform :ios, '8.0'
use_frameworks!#个别需要用到它，比如reactiveCocoa

target ‘百思不姐’ do
  pod "DACircularProgress"
  pod "AFNetworking"
  pod "SDWebImage"
  pod "MBProgressHUD"
  pod "MJRefresh"
  pod 'MJExtension'
  pod 'pop', '~> 1.0.9'
end
